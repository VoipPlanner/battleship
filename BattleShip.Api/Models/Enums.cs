namespace BattleShip.Api.Models
{
    public enum Alignment
    {
        Horizontal,
        Vertical
    }

    public enum GameStatus
    {
        Setup,
        Playing,
        NotStarted
    }
}